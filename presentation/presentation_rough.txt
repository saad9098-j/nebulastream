Notes for Later:

Suggested Structure (4 Speakers)
Section	Speaker	Duration	Content
1. Recap & Progress Overview	Person 1	3 min	Recap kick-off goals, high-level achievements
2. LinuxProcessSource Deep Dive	Person 2	5 min	Implementation details, code walkthrough
3. LinuxProcessSink Deep Dive	Person 3	5 min	Implementation details (only CMakeFile.txt), plan
4. Demo & Next Steps	Person 4	5 min	Live demo, testing plan, timeline




Slides:

slide 1:

Data Systems Project - Progress Update

Linux Process Source & Sink
Connecting NebulaStream to the Linux Ecosystem

Team Members:
- Saad Aldeen Mnowar • 495171
- Roshni Ajay Melwani • 475317
- Emi Maliqi • 498273
- Aleksandar Georgiev • 468867

Date: [Current Date]

slide 2:

From Kick-off Presentation (01.12.2025):

Challenge:
• NebulaStream primarily ingests data from static files or network sockets
• Limited direct interaction with local system tools

Our Solution:
• LinuxProcessSource → Execute scripts, ingest stdout
• LinuxProcessSink → Pipe results to external processes' stdin

slide 3:
✅ COMPLETED                                             📋 PLANNED
──────────────────────────────────────────────────────────────────────────────────────────────
✅ LinuxProcessSource                                    📋 LinuxProcessSink implementation
   • Header (Class)                                         • Header (Class)
   • implementation (open(), fillTupleBuffer(), close())    • implementation (start(), execute(), stop())
   • CMake (source plugin, validation plugin)

✅ Demo scripts & SQL examples                           📋 Documentation (if needed)
   • producer.py                                            • Usage guide
   • consumer.py                                            • API reference
   • demo.sql queries
                                                         📋 Testing Suite
                                                            • Unit tests
                                                            • Integration tests

                                                         📋 Code Quality
                                                            • Remove & add debug statements
                                                            • Clean up commented code
                                                            • Code review & refactoring

                                                         📋 Size limiting for process output

───────────────────────────────────────────────────────────────────────────────────────────────

1. Remove & Debug Statements
   • better NES_DEBUG/NES_TRACE/NES:INFO statements

2. Code Quality
   • Remove commented-out code blocks
   • Review error messages for clarity and tests
   • Maximize efficiency when possible

slide 4:
┌─────────────────────────────────────────────────────────────┐
│                    NebulaStream Engine                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌──────────────────┐    ┌──────────┐    ┌──────────────┐ │
│   │ LinuxProcess     │    │  Query   │    │ LinuxProcess │ │
│   │ SOURCE           │───▶│ Pipeline │───▶│ SINK         │ │
│   │                  │    │          │    │              │ │
│   │ popen("cmd","r") │    │ SELECT,  │    │popen("cmd","w")│
│   │                  │    │ WHERE,   │    │              │ │
│   └────────┬─────────┘    │ GROUP BY │    └──────┬───────┘ │
│            │              └──────────┘           │         │
└────────────│─────────────────────────────────────│─────────┘
             │                                     │
     ┌───────▼───────┐                     ┌───────▼───────┐
     │ External      │                     │ External      │
     │ Process       │                     │ Process       │
     │ (producer.py) │                     │ (consumer.py) │
     └───────────────┘                     └───────────────┘




slide 5: LinuxProcessSource

File Structure:
nes-plugins/Sources/LinuxProcessSource/
├── LinuxProcessSource.hpp   (79 lines)
├── LinuxProcessSource.cpp   (146 lines)
└── CMakeLists.txt           (38 lines)

Key Components:
• Structure: ConfigParametersLinuxProcessSource::COMMAND
• open(): → starts an external producer process, establishes a unidirectional data flow, data flows from process → NebulaStream, implemented via:
void LinuxProcessSource::open(...) {
    // Debug/Info statements
    pipe = popen(commandToRun.c_str(), "r");
    // Error handling logic
}
• fillTupleBuffer() → read from stdout
• close() → close the process
void LinuxProcessSource::close()
{
    if (pipe)
    {
        pclose(pipe);
        pipe = nullptr;
        // Debug/Info statements
    }
    // Error handling logic
}
• Registry hooks and validation


slide 6: LinuxProcessSink

File Structure:
nes-plugins/Sinks/LinuxProcessSink/
└── CMakeLists.txt   ✅ Done
├── LinuxProcessSink.hpp   📋 Planned
└── LinuxProcessSink.cpp   📋 Planned

Key Components:
• Structure: ConfigParametersLinuxProcessSink::COMMAND
• start(): → Spawn consumer process
• execute() → Write tuples to stdin
• close() → Terminate process


slide 7-8: example python scripts
# producer.py - Generates data stream
for x in [1, 2, 3, 4]:
    print(x, flush=True)

# consumer.py - Receives and processes data
import sys
for line in sys.stdin:
    print("ACTUATOR RECEIVED:", line.strip())


slide 7-8: SQL-Demos for single node workers
• Linux process source that executes a python script and reads from it and a file sink:
CREATE LOGICAL SOURCE demo(value UINT64);
CREATE PHYSICAL SOURCE FOR demo TYPE LinuxProcess SET(
  'python3 /home/saad9098/nebulastream/producer.py' AS `SOURCE`.command,
  'CSV' AS PARSER.`TYPE`,
  '\n' AS PARSER.TUPLE_DELIMITER,
  ','  AS PARSER.FIELD_DELIMITER
);
CREATE SINK result(demo.value UINT64) TYPE File SET(
  './demo-output.csv' AS `SINK`.FILE_PATH,
  'CSV' AS `SINK`.INPUT_FORMAT
);
SELECT value FROM demo INTO result;

• File source and linux process sink that executes a python script and writes for it:
CREATE LOGICAL SOURCE demo(value UINT64);
CREATE PHYSICAL SOURCE FOR demo TYPE File SET('./demo-input.csv' AS `SOURCE`.FILE_PATH, 'CSV' AS PARSER.`TYPE`, '\n' AS PARSER.TUPLE_DELIMITER, ',' AS PARSER.FIELD_DELIMITER);
CREATE SINK result(demo.value UINT64) TYPE LinuxProcess SET(
  'python3 /home/saad9098/nebulastream/consumer.py' AS `SINK`.command,
  'CSV' AS `SINK`.INPUT_FORMAT
);
SELECT value FROM demo INTO result;

• Linux process source and sink:
CREATE LOGICAL SOURCE demo(value UINT64);

CREATE PHYSICAL SOURCE FOR demo TYPE LinuxProcess SET(
  'python3 /home/saad9098/nebulastream/producer.py' AS `SOURCE`.command,
  'CSV' AS PARSER.`TYPE`,
  '\n' AS PARSER.TUPLE_DELIMITER,
  ','  AS PARSER.FIELD_DELIMITER
);

CREATE SINK result(demo.value UINT64) TYPE LinuxProcess SET(
  'python3 /home/saad9098/nebulastream/consumer.py' AS `SINK`.command,
  'CSV' AS `SINK`.INPUT_FORMAT
);

SELECT value FROM demo INTO result;

slide 9: Plan

Week 1: LinuxProcessSink Implementation 1
┌────────────────┬────────────────────────────────────────────┐
│ Saad & Aleks   │ LinuxProcessSink.hpp                       │
│                │ - Class definition & config parameters     │
├────────────────┼────────────────────────────────────────────┤
│ Roshni & Emi   │ LinuxProcessSink.cpp - Core logic          │
│                │ - start(), execute(), stop()               │
└────────────────┴────────────────────────────────────────────┘


Week 2: LinuxProcessSink Implementation 2
┌────────────────┬────────────────────────────────────────────┐
│ Saad & Aleks   │ Formatter integration & validation         │
│                │ - CSV/JSON output formatting               │
├────────────────┼────────────────────────────────────────────┤
│ Roshni & Emi   │ Registry hooks & Validation & demo queries │
└────────────────┴────────────────────────────────────────────┘
┌────────────────┬────────────────────────────────────────────┐
│ All            │ Unit tests for Source & Sink               │
│                │ Integration tests                          │
│                │ System-level tests (.test files)           │
└────────────────┴────────────────────────────────────────────┘

Week 3-4: Testing, Cleaning, Debugging
┌────────────────┬────────────────────────────────────────────┐
│ All            │ Cleaning and adding debugging statements   │
│                │ Unit tests for Source & Sink               │
│                │ Integration tests                          │
└────────────────┴────────────────────────────────────────────┘

Week 5: Documentation & Final Presentation
┌────────────────┬────────────────────────────────────────────┐
│ All            │ combine Documentations                     │
│                │ our Reflection                             │
│                │ Prepare final presentation                 │
└────────────────┴────────────────────────────────────────────┘

slide 10:
Questions?

Repository: nebulastream/nebulastream
Branch: linux-processs-branch

Team Contact:
• Saad Aldeen Mnowar
• Roshni Ajay Melwani  
• Emi Maliqi
• Aleksandar Georgiev
